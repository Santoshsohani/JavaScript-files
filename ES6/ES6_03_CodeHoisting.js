var getTemp = (temp,cold = 10) => {
    if (temp < cold) {
        console.log("This is cold here");
    } else {
        console.log("This is not so cold");
    }
}

// code Hoisting is we should declare the variable first and then use it.

